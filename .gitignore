# Build
bin/
obj/

# Visual Studio
.vs/
*.user
*.suo
*.sln
*.userosscache

# JetBrains Rider
.idea/

# Données utilisateur (runtime) — JAMAIS dans le repo ni dans le ZIP de MAJ.
# Depuis v1.2.8 elles vivent dans config\ à côté de l'exe ; les patterns à plat
# couvrent l'ancien layout (≤ v1.2.7) par sécurité.
config/
tweakly-settings.json
tweakly-benchmarks.json
tweakly-bench-ref.json
tweakly-log.txt
tweakly-log.txt.old

# Script d'installeur + dossier de packaging (pas nécessaire pour build / run)
installer/
release-stage/
_watchdog_publish_test/

# Artefacts locaux d'audit et de réparation — jamais versionnés.
# Les images WIM/UUP et les sorties de tests restent hors du dépôt propre.
_repair_output/
RepairCache/
.candidate-repair*
_uup_payload_*/
_uup_audit/
_audit_validation/
_codex_*/
_codex_*.ps1
_gpu_audit_build/
_icon_build/
_registry_build/
_repair_test_build/
_psf_marker_test_*/
_repair_wim_tool/

# OS
Thumbs.db
.DS_Store
